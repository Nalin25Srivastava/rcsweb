const mongoose = require('mongoose'); const Contact = require('./backend/models/Contact'); require('dotenv').config({path: './backend/.env'}); mongoose.connect(process.env.MONGO_URI).then(async () => { const count = await Contact.countDocuments(); console.log('Count in Contact model:', count); const db = mongoose.connection.db; const collections = await db.listCollections().toArray(); console.log('Collections:', collections.map(c => c.name)); process.exit(0); });