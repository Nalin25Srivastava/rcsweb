const mongoose = require('mongoose'); require('dotenv').config(); mongoose.connect(process.env.MONGO_URI).then(async () => { const db = mongoose.connection.db; const collections = await db.listCollections().toArray(); console.log('Collections:', collections.map(c => c.name)); const messages = await db.collection('messages').find().toArray(); console.log('Messages in db:', messages); process.exit(0); });